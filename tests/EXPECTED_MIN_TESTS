# The BASE of the host test-count floor (#390, #467).
# Checked by tests/tools/check_test_count.py after the unit-test step.
#
# The floor is this number PLUS every tests/min_tests.d/<issue>-<slug>.txt.
# Do NOT edit this number in a PR that adds tests -- add a contribution file
# instead (see tests/min_tests.d/README.md), so two PRs never touch one line.
# Only a compaction PR on main changes it: check_test_count.py --compact.
#
# Set to the count on main @ e2d00c5 (after #407), measured by BigDog:
# 1254 tests from 175 suites.
1254
